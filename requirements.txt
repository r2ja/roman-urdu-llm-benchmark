# Core
requests>=2.31
python-dotenv>=1.0
PyYAML>=6.0

# Metrics
sacrebleu>=2.4          # chrF++, char-BLEU, BLEU
scikit-learn>=1.3       # macro-F1, accuracy

# Optional (heavier, not required for --selftest)
# unicodedata is stdlib; no extra transliteration lib needed for v1
